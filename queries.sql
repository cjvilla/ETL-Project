create table us_data (
	id int primary key,
	neighbourhood text,
	room_type text,
	price int,
	minimum_nights int,
	city text
);

create table chicago_data (
	id int primary key,
	neighbourhood text,
	room_type text,
	price int,
	minimum_nights int,
	city text
);
